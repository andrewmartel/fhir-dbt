# Copy to .env and set your Postgres password. Used by the loader and by run_everything.py (which passes it to dbt as DBT_PASSWORD).
# Do not commit .env — it is in .gitignore.
PGHOST=localhost
PGPORT=5432
PGDATABASE=fhir
PGUSER=postgres
PGPASSWORD=your_password_here
